package ar.edu.unq.domino.test

import ar.edu.unq.domino.Pizzas.Pedido
import ar.edu.unq.domino.formasDeEnvio.Delivery
import ar.edu.unq.domino.formasDeEnvio.FormaDeRetiro
import ar.edu.unq.domino.repo.RepoPedidos
import ar.edu.unq.domino.sistema.Cliente
import org.junit.Before
import org.junit.Test
import static org.junit.Assert.*

class RepoPedidosTest {
/*	RepoPedidos repoPedidos
	Cliente nahuel
	Pedido pedido
	FormaDeRetiro formaDeRetiro

	@Before
	def void init() {
		repoPedidos = RepoPedidos.instance

		formaDeRetiro = new Delivery
		nahuel = new Cliente("Nahuel", "Nahuelpm", "sarasa123", "Shamainco1@gmail.com", "Av.Falsa 1234")
		pedido = new Pedido(nahuel)
		pedido.setFormaDeRetiro(formaDeRetiro)
	}

	@Test
	def void pedidoEnLaListaPedidos() {
		val resultado = repoPedidos.getPedidos
		repoPedidos.CrearPedidoNuevo(pedido)
		assertEquals(resultado.size(), 1)
	}

	@Test
	def void SacarPedidoDeLaListaPedidos() {
		val resultado = repoPedidos.getPedidos
		repoPedidos.CrearPedidoNuevo(pedido)
		repoPedidos.eliminarPedido(1)
		assertEquals(resultado.size(), 0)
	} */
	
}
